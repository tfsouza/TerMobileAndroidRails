= semantic_form_for [@cursodisciplina] do |form|
   
  = form.input :curso, :member_label => :nome, :label => 'Curso' 
  = form.input :disciplina, :member_label => :sigla, :label => 'Disciplina'
  = form.submit 'Salvar'

